generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model User {
  id            String     @id @default(cuid())
  name          String?
  email         String?    @unique
  emailVerified DateTime?
  password      String?
  role          String     @default("USER")
  image         String?
  createdAt     DateTime   @default(now())
  updatedAt     DateTime   @updatedAt
  purchases     Purchase[]
}

model BotProduct {
  id              String     @id @default(cuid())
  name            String
  description     String
  instrument      String
  strategyType    String
  riskLevel       String
  price           Float
  version         String     @default("1.0")
  ex5FilePath     String?
  pdfFilePath     String?
  isActive        Boolean    @default(true)
  status          String     @default("ACTIVE")
  timeframes      String?
  minCapital      Float?
  backtestResults String?
  createdAt       DateTime   @default(now())
  updatedAt       DateTime   @updatedAt
  purchases       Purchase[]
}

model Purchase {
  id                    String     @id @default(cuid())
  userId                String
  botProductId          String
  amount                Float
  status                String     @default("COMPLETED")
  lastDownloadedVersion String?
  createdAt             DateTime   @default(now())
  expiresAt             DateTime?
  botProduct            BotProduct @relation(fields: [botProductId], references: [id])
  user                  User       @relation(fields: [userId], references: [id])
  remoteCommands        RemoteCommand[]

  @@index([botProductId], map: "Purchase_botProductId_fkey")
  @@index([userId], map: "Purchase_userId_fkey")
}

model RemoteCommand {
  id         String   @id @default(cuid())
  purchaseId String
  command    String   // PAUSE, RESUME, CLOSE_ALL, CHANGE_MODE, DIRECTION
  value      String?  // ZEN, COSECHA, BUY, SELL, etc.
  isExecuted Boolean  @default(false)
  createdAt  DateTime @default(now())
  purchase   Purchase @relation(fields: [purchaseId], references: [id])

  @@index([purchaseId])
}
